
# Functional Testing definition model for Shaddock.
# =================================================
# Jinja2 rendering testing

---

clusters: 

  - name: vars-cluster1
    vars: 
      !include site01/test-variables01.yml
    vars:
      !include site01/test-variables02.yml
    images: images/testdir
    services:
      - name: env-test
        image: testuser/arch_base:latest
        priority: 400
        env:
          VAR001: '{{ var_001 }}'
        command: "env"
