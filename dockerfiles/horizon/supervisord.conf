[supervisord]
nodaemon=true

#[program:sshd]
#command=/usr/sbin/sshd -D

[program:httpd]
command=/bin/bash -c "source /etc/httpd/envvars && exec /usr/sbin/httpd -DFOREGROUND"
autorestart=true
priority=30

[program:memcached]
command=/usr/bin/memcached -u daemon
autorestart=true
priority=30
